export default function TopBar() {
    return (
        <header className="topbar">
            <strong>single-spa POC</strong>
            <span className="topbar-subtitle">root-config (shell)</span>
        </header>
    );
}
